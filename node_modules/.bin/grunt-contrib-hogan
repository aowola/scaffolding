#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-contrib-hogan/bin/grunt-contrib-hogan" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-contrib-hogan/bin/grunt-contrib-hogan" "$@"
  ret=$?
fi
exit $ret
